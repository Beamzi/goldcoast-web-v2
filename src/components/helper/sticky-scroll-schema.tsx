const stickyScrollSchema = (
  title: string,
  description: string,
  content: React.ReactNode | any
) => {
  return {
    title: title,
    description: description,
    content: content,
  };
};

export default stickyScrollSchema;
